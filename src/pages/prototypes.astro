---
import HomepagePrototypes from '../components/HomepagePrototypes.svelte';
import BackgroundWidget from '../components/BackgroundWidget.svelte';

const title = "Homepage Prototypes — Hogg Country";
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Caveat:wght@400;500;600;700&family=Oswald:wght@400;500;600;700&display=swap" rel="stylesheet" />
    <style is:global>
      *, *::before, *::after {
        box-sizing: border-box;
      }

      html {
        height: 100%;
      }

      body {
        margin: 0;
        min-height: 100%;
        font-family: system-ui, -apple-system, sans-serif;
        -webkit-font-smoothing: antialiased;
        background-color: #f5f2e8;
        background-repeat: no-repeat;
        background-attachment: fixed;
      }

      :root {
        --pine: #4d594a;
        --alpine: #a6b589;
        --terra: #d97706;
        --marker: #f0e000;
        --bg: #f5f2e8;
        --ink: #2b2f26;
        --stone: #cccccc;
        --border: #e6e1d4;
        --muted: #5c665a;
      }
    </style>
  </head>
  <body>
    <HomepagePrototypes client:load />
    <BackgroundWidget client:only="svelte" />
  </body>
</html>
