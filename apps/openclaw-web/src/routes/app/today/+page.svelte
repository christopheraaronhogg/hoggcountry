<!--
  Legacy route shell. +page.server.ts redirects every request to /app, so this
  component is unreachable — it only exists because SvelteKit needs a +page.svelte
  for the route to resolve at all.
-->
