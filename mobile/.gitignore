node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test
# Tracked: production build policy (no secrets — only the public VITE_ model
# policy that makes on-device builds Gemma-only). Loaded by `vite build`.
!.env.production

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
